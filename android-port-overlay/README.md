# Android Port Overlay

Use this directory for Android-specific glue and patch files that adapt the upstream RPCS3 source tree for the Android NDK build.

Recommended contents:

- Vulkan surface shims
- input and gamepad adapters
- Android filesystem helpers
- small build patches that avoid deep long-term forks
